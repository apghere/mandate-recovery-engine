[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mandate-recovery-engine"
version = "0.1.0"
description = "Constrained retry-budget planner for Indian recurring-payment mandates"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "scikit-learn>=1.5",
    "numpy>=1.26",
    "matplotlib>=3.9",
    "anthropic>=0.40",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "hypothesis>=6.100",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-PyYAML>=6.0",
]

[tool.setuptools.packages.find]
where = ["backend"]

[tool.pytest.ini_options]
pythonpath = ["backend", "."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "backend"
files = ["backend/app", "data", "simulator", "scripts", "evaluation"]

# scikit-learn ships no type stubs / py.typed marker. matplotlib has ships
# its own (py.typed) so isn't listed here.
[[tool.mypy.overrides]]
module = ["sklearn.*"]
ignore_missing_imports = true
